name: morning (08:30 JST)
on:
  schedule:
    - cron: "30 23 * * 0-4"
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 120 "${{ secrets.RENDER_URL }}/summary/morning?secret=${{ secrets.SUMMARY_SECRET }}"
